# Platform compatibility path

`src/platform/core` has been relocated to `src/core/cores/gba/platform` so the
GBA platform layer is maintained with the GBA core.

This directory is kept as a compatibility entry point.
