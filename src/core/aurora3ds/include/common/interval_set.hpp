#pragma once

// Keep this header as a compatibility include path while sourcing the real implementation
// from the sys/common reference location to avoid divergence.
#include "../../src/core/sys/common/interval_set.hpp"
