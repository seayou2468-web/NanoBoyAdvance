#pragma once

#include <string>

namespace FileUtil {

inline constexpr char DIR_SEP_CHR = '/';
inline constexpr const char* DIR_SEP = "/";

} // namespace FileUtil

inline constexpr const char* SHARED_FONT = "SharedFontReplacement.bin";
