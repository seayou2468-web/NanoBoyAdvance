#pragma once

// iOS standalone Mikage build does not use libretro core options.
// Keep this header to satisfy cfg service includes without adding external dependencies.

#include "../common/settings.h"
